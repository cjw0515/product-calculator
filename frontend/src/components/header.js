import React, { Component } from 'react';
import Bar from './bar';

class Header extends Component {
    render(){
        return(
            <div>
                <Bar/>
            </div>
        )
    }
}

export default Header;